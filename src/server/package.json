{
  "name": "server",
  "version": "1.0.0",
  "description": "server for crud operations",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Vibhanshu",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.16.3"
  }
}
